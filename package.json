{
  "name": "@mersennet/sdk",
  "version": "0.1.0",
  "description": "TypeScript SDK for Mersennet",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "pnpm build && node --test tests/"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^24.10.1",
    "typescript": "^5.0.0"
  }
}
